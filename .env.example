# Copy to .env and fill in whichever providers you use.
# Keys can ALSO be entered directly in each provider window in the UI.

OPENAI_API_KEY=
OPENROUTER_API_KEY=
KIMI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: force a base URL for OpenAI-compatible gateways (rarely needed).
# OPENAI_BASE_URL=
